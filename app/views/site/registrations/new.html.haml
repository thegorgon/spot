-set_page_title "Register for #{@event.name}"
-set_spot_app_title "Register"

=content_for :toptitle do
  ="Register for #{@event.name}"
  
#claimdialog.clearfix
  =render :partial => "/site/events/claim", :locals => {:event => @event}