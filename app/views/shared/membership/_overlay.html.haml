-describe = defined?(describe) ? describe : false
-title ||= "Get access to all these unforgettable experiences"
-unless current_member?
  .overlay.clearfix
    -if current_user.try(:membership_application)
      .title.clearfix
        -if current_user.membership_application.approved?
          %h1.tf.left.membership
            Congratulations! Your application has been approved.
          =link_to "complete", new_membership_path, :class => "btnorg170x41 tf right"
        -else
          %h1.tf.left.membership
            We've received your application and are reviewing it
          =link_to "check status", application_path, :class => "btnorg170x41 tf right"           
    -else
      -if describe
        .content.clearfix
          .left.column
            %h1.tf
              Membership.
            %p 
              / Membership has its privileges. Our exclusivity allows us to secure VIP experiences, available to no one else.
              Limited to 10,000 people like you. People who restaurants want to impress.
          
          .right.column
            %h1.tf
              Experiences.
            %p 
              Chef’s tables, secret menus, kitchen tours, special wine pairings, private tastings&hellip;available only to Spot members.
        .clear.btn
          =button_tag :class => "btnorg170x41 btnmembership tf centered" do
            apply now
      -else
        .title.clearfix
          %h1.tf.left.membership
            =title
          =button_tag :class => "btnorg170x41 btnmembership tf right" do
            apply now
