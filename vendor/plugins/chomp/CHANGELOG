0.4.0
  * Duplicate SSL CSS assets are not generated if identical to non-SSL version.
  * Added settings section to chomp.yml, added settings for javascripts_public_dir & stylesheets_public_dir.
  * Safer cache cleans -- warns if cache directories contain non-optimized assets.
  
0.3.0
  * Initial support for SSL assets (and asset-rewriting).
  
0.2.0
  * Added support for nested asset groups (e.g., the :defaults group may include the :prototype group).

0.1.0
  * Initial (working) prototype.